<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4">
  <a class="navbar-brand fw-bold" href="/">📘 Jobs EJS App</a>
  

  <div class="collapse navbar-collapse justify-content-end">
    <% if (user) { %>
      <ul class="navbar-nav me-3">
        <li class="nav-item">
          <span class="nav-link text-light">👤 Logged in as <strong><%= user.name %></strong></span>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/lessons">📚 Manage Lessons</a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="/lessons/explore">🌍 Explore</a> <!-- ✅ ADDED -->
        </li>
        
        <li class="nav-item">
          <a class="nav-link" href="/secretWord">🔐 Secret Word</a>
        </li>
      </ul>
      <form class="d-flex" method="POST" action="/sessions/logoff">
        <input type="hidden" name="_csrf" value="<%= _csrf %>">
        <button class="btn btn-outline-light btn-sm" type="submit">🚪 Logoff</button>
      </form>
    <% } else { %>
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/sessions/logon">🔑 Logon</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/sessions/register">📝 Register</a>
        </li>
      </ul>
    <% } %>
  </div>
</nav>

<% if (errors && errors.length > 0) { %>
  <% errors.forEach(err => { %>
    <div class="alert alert-danger text-center m-0"><%= err %></div>
  <% }); %>
<% } %>

<% if (info && info.length > 0) { %>
  <% info.forEach(msg => { %>
    <div class="alert alert-success text-center m-0"><%= msg %></div>
  <% }); %>
<% } %>
